function showDishDetails(dishName, price) {
    document.getElementById('selected-dish-name').textContent = dishName;
    document.getElementById('selected-dish-price').textContent = `Price: ${price}`;
}
